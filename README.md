**Test assignment for Starred.**

Make sure to install prerequisites:
- Docker
- GNU Make

To build use:
    `make`

To start use:
    `make start`

The web server will be running using the hostname provided at the top of Makefile.